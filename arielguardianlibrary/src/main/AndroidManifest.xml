<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ariel.guardian.library">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <service android:name=".pubnub.PubNubService"
            ></service>

        <service android:enabled="true" android:name=".services.SyncIntentService" />

        <meta-data android:name="DATABASE" android:value="ariel.db" />
        <meta-data android:name="VERSION" android:value="1" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.ariel.guardian.library.db.model" />

    </application>

</manifest>
